package eastereggs.service;

public interface IGrid {

	void collectEgg(int x, int y, IRabbit iRabbit);
	//int collectEgg(IPosition iPosition);
	//Boolean isValid(IPosition iPosition);
	//Boolean isValid(int x, int y);
	
}
